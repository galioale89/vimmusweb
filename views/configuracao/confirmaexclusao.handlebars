<div class='caixa-confirma'>
    <div class="card-body">
        <div class="row" style="margin-top: 30px;">
            <h4 class="text-center mb-4">Você tem certeza que deseja excluir esta Empresa</h4>
            <h2 class="text-center mb-4">{{empresa.nome}}</h2>
            <div class="row">
                <div class='btn-group btn-group-lg gap-2' role='group'>
                    <a class='btn btn-danger' href='/configuracao/removeempresa/{{empresa._id}}'>Sim</a>
                    <a class='btn btn-secondary' href='/configuracao/consultaempresa'>Não</a>
                    <a class='btn btn-primary' href='/configuracao/consultaempresa'>Voltar</a>
                </div>
            </div>
        </div>
    </div>
</div>